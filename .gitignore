# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Rust / Tauri
src-tauri/target/
src-tauri/gen/

# Build output
*.dmg

# LaTeX build output. The editor writes everything to build/, so that line does
# most of the work — the rest catches output from a manual pdflatex run.
build/
*.aux
*.out
*.toc
*.fls
*.fdb_latexmk
*.synctex.gz
*.bbl
*.blg

# Local app state. API keys live in ~/.latex-editor/settings.json, outside the
# repo by design — this is belt and braces in case that ever moves.
.latex-editor/
